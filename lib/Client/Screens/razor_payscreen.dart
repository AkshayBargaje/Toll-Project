import 'package:flutter/material.dart';

class RazorPayScreen extends StatelessWidget {
  const RazorPayScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Razor page page"),
    );
  }
}
